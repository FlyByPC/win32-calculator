{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "WinCalc (Debug)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\build\\WinCalc.exe",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build WinCalc (MSVC)",
            "stopAtEntry": false
        }
    ]
}
